---
numbering: false
---
(appendix:solutions)=
# Solutions to exercises

<!--
PRINT ONLY. This page is deliberately absent from the project `toc` in
myst.yml and appears only in the pdf export's `articles:` list. On the web,
solutions stay next to their exercise as `:class: dropdown` blocks, which the
reader opens when ready; a printed book has no disclosure triangle, so the
solutions are gathered here instead and the chapter carries a pointer.

Each solution below is pulled in from its chapter by label. When you add a
`{solution}` to a chapter, add its `:label:` here too, otherwise it will not
appear in the PDF at all. `_plugins/latex-blocks.mjs` strips the inline copy
during the PDF build and replaces it with a reference to this appendix.
-->

```{draft}
```

:::{embed} #2_1_exercise_1_solution
:::

:::{embed} #2_2_solution_surrogate
:::

:::{embed} #2_2_solution_eval
:::
